{
  "name": "smart-vote",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@apollo/client": "^3.7.14",
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "apollo-server-express": "^3.12.0",
    "cloudinary": "^1.37.0",
    "express": "^4.18.2",
    "graphql": "^16.6.0",
    "jsonwebtoken": "^9.0.0",
    "jwt-decode": "^3.1.2",
    "kill-port": "^2.0.1",
    "mongod": "^2.0.0",
    "mongoos": "^0.0.1-security",
    "mongoose": "^7.1.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.11.1",
    "react-scripts": "5.0.1",
    "serve": "^14.2.0",
    "web-vitals": "^2.1.4"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.22",
    "tailwindcss": "^3.3.2"
  },
  "scripts": {
    "start": "concurrently \"cd server && npm run start\" \"cd client && react-scripts start\"",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
    "install": "npm i --dev && cd server && npm i --dev && cd ../client && npm i --dev",
    "build": "cd client && npm run build",
    "test": "react-scripts test",
    "seed": "cd server && npm run seed",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
